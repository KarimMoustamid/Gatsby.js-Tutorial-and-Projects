import React from 'react';
import Layout from "../components/Layout";

const Recipes = () => {
    return (
        <div>
            <Layout>
                <h1>Recopies</h1>
            </Layout>
        </div>
    );
};

export default Recipes;